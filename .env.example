# Optional override for API base URL.
#
# Development (if your backend is on a different host/port):
# VITE_API_URL=http://localhost:4000/api
#
# Production behind Nginx reverse proxy: leave unset (frontend defaults to /api)
VITE_API_URL=http://localhost:4000/api
